#!/usr/bin/env bash
set -e
if [[ ! -d "/nas/projects/web/personal-site/frontend" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/nas/projects/web/personal-site/frontend")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/nas/projects/web/personal-site/frontend" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/nas/projects/web/personal-site/frontend/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/nas/projects/web/personal-site/frontend/.envrc" "/nas/projects/web/personal-site/frontend/.direnv"/*.rc
